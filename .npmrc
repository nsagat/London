# Install devDependencies during the build even when NODE_ENV=production
# (Railway sets NODE_ENV=production, which would otherwise omit typescript,
# tailwindcss, and postcss — all required by `next build`).
production=false
